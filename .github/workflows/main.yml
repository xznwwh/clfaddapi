name: Daily Job

# on:
#   schedule:
#     - cron: '0 8 * * *'  # 每日凌晨 2 点执行
    
on: workflow_dispatch


jobs:
  run_daily_job:
    runs-on: ubuntu-latest
    
    steps:
      - name: Git checkout
        uses: actions/checkout@v3
        with:
            fetch-depth: 0
        
      - name: Setup Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.8.18
        
      - name: Add and Commit File
        run: |
          git config --local user.email "528451599@qq.com"
          git config --local user.name "AomoDa"

 
      - name: Run Python Script
        run: |
          python -m pip install --upgrade pip
          pip install requests
          python main.py
          
      - name: GIT COMMIT
        run: |          
          git add clfgoodip.csv
          git commit -m "update"
          
      - name: Push Changes
        uses: ad-m/github-push-action@v0.6.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}          
